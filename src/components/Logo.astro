---
// Florecer Mind Logo - Minimalist seed sprouting design
---

<div class="flex items-center gap-3">
    <svg width="40" height="50" viewBox="0 0 40 50" fill="none" xmlns="http://www.w3.org/2000/svg" class="text-primary">
        <!-- Seed (perfect circle) -->
        <circle cx="20" cy="35" r="8" fill="currentColor" />

        <!-- Stem (curved growth line) -->
        <path
            d="M 20 35 Q 20 20, 25 10"
            stroke="currentColor"
            stroke-width="2.5"
            fill="none"
            stroke-linecap="round"
        />

        <!-- Leaf (simple curved shape) -->
        <path
            d="M 25 10 Q 32 8, 35 12 Q 32 16, 25 14 Z"
            fill="currentColor"
        />
    </svg>

    <div class="flex flex-col">
        <span class="text-xl font-bold tracking-wide text-primary">FLORECER MIND</span>
        <span class="text-xs tracking-wider text-neutral-600">Where Research Blooms into Wisdom</span>
    </div>
</div>
